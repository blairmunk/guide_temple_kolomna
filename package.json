{
  "name": "guide_temple_kolomna",
  "private": true,
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "apps/web",
    "apps/api"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel --filter=web --filter=api",
    "build": "turbo run build",
    "test": "turbo run test",
    "init-mvp": "bash init-mvp.sh"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "pg": "^8.11.0"
  }
}
